[project]
name = "rag-core"
version = "0.1.0"
description = "Core domain and application layer for RAG pipeline"
requires-python = ">=3.11"
license = { text = "MIT" }

# No external dependencies - pure Python only
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rag_core"]
